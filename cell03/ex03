def print_multiplication_tables():
    for i in range(11):
        result = []
        j = 0
        while j <= 10:
            result.append(i * j)
            j += 1
        print(f"Table de {i}: {' '.join(map(str, result))}")

if __name__ == "__main__":
    print_multiplication_tables()
